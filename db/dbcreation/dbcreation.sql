-- CREATE DATABASE prefocus;

CREATE TABLE focusing (
    date varchar(255),
    prefocus longtext,
    id varchar(255)
);

CREATE TABLE focused (
    date varchar(255),
    prefocus longtext,
    id varchar(255)
);
